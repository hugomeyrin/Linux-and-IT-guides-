Update apt database with apt-get using the following command.

sudo apt-get update

After updating apt database, We can install pulseeffects using apt-get by running the following command:

sudo apt-get -y install pulseeffects

Install pulseeffects Using apt

Update apt database with apt using the following command.
Ezoic

sudo apt update

After updating apt database, We can install pulseeffects using apt by running the following command:Ezoic

sudo apt -y install pulseeffects

Install pulseeffects Using aptitude

If you want to follow this method, you might need to install aptitude first since aptitude is usually not installed by default on Debian. Update apt database with aptitude using the following command.

sudo aptitude update

After updating apt database, We can install pulseeffects using aptitude by running the following command:Ezoic

sudo aptitude -y install pulseeffects

How To Uninstall pulseeffects on Debian 12

To uninstall only the pulseeffects package we can use the following command:

sudo apt-get remove pulseeffects

Uninstall pulseeffects And Its Dependencies

To uninstall pulseeffects and its dependencies that are no longer needed by Debian 12, we can use the command below:Ezoic

sudo apt-get -y autoremove pulseeffects

Remove pulseeffects Configurations and Data

To remove pulseeffects configuration and data from Debian 12 we can use the following command:

sudo apt-get -y purge pulseeffects

Remove pulseeffects configuration, data, and all of its dependencies

We can use the following command to remove pulseeffects configurations, data and all of its dependencies, we can use the following command:Ezoic

sudo apt-get -y autoremove --purge pulseeffects

Dependencies

pulseeffects have the following dependencies:

    gir1.2-gst-plugins-bad-1.0
    gstreamer1.0-adapter-pulseeffects
    gstreamer1.0-plugins-bad
    gstreamer1.0-plugins-good
    gstreamer1.0-pulseaudio
    pulseaudio
    dconf-gsettings-backend
    libatkmm-1.6-1v5
    libboost-filesystem1.74.0
    libc6
    libcairomm-1.0-1v5
    libgcc-s1
    libglib2.0-0
    libglibmm-2.4-1v5
    libgstreamer-plugins-base1.0-0
    libgstreamer1.0-0
    libgtk-3-0
    libgtkmm-3.0-1v5
    libpangomm-1.4-1v5
    libpulse0
    libsigc++-2.0-0v5
    libsndfile1
    libstdc++6

